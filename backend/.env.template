# ── Required ────────────────────────────────────────────────────────────
DATABASE_URL=postgres://postgres:postgres@localhost:5432/coast
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-to-a-long-random-string
COOKIE_SECRET=change-me-to-another-long-random-string

# Public URL of this backend (used by the admin dashboard)
MEDUSA_BACKEND_URL=http://localhost:9000

# CORS: storefront URL for STORE_CORS; backend URL for ADMIN/AUTH
STORE_CORS=http://localhost:8000
ADMIN_CORS=http://localhost:9000
AUTH_CORS=http://localhost:9000

# ── First admin user (created on first boot if it doesn't exist) ─────────
MEDUSA_ADMIN_EMAIL=
MEDUSA_ADMIN_PASSWORD=

# ── Stripe (leave empty until ready; checkout stays in test/off mode) ────
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=
